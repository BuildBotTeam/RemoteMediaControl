name: remote_media_control
description: A new Flutter project.

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.1.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  keypress_simulator: ^0.1.0
  system_tray: ^2.0.3
  window_manager: ^0.3.7
  network_info_plus: ^4.1.0
  launch_at_startup: ^0.2.2
  package_info_plus: ^4.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0


flutter:
  uses-material-design: true

  assets:
    - assets/
